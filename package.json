{
  "name": "backend",
  "version": "1.0.50",
  "scripts": {
    "format": "bun x biome format src/ --fix",
    "lint": "bun x biome lint src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "prepare": "husky install || true"
  },
  "dependencies": {
    "elysia": "latest",
    "inversify": "^6.2.2",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
